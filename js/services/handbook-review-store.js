import { PUBLIC_CONFIG } from '../../config.js';
import { currentSession } from './auth-service.js';
const KEY='ravradar-handbook-review-drafts-v1';
const enabled=Boolean(PUBLIC_CONFIG.supabaseUrl&&PUBLIC_CONFIG.supabasePublishableKey);
function drafts(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}}
function save(rows){localStorage.setItem(KEY,JSON.stringify(rows));}
function id(){return crypto.randomUUID?.()||`review-${Date.now()}-${Math.random().toString(36).slice(2)}`;}
export function centralReviewStorageEnabled(){return enabled&&Boolean(currentSession()?.access_token);}
async function remoteInsert(review){const s=currentSession();if(!enabled||!s?.access_token)throw new Error('Login kræves');const r=await fetch(`${PUBLIC_CONFIG.supabaseUrl}/rest/v1/handbook_reviews`,{method:'POST',headers:{apikey:PUBLIC_CONFIG.supabasePublishableKey,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json',Prefer:'return=representation'},body:JSON.stringify({id:review.id,handbook_version:review.handbookVersion,section_id:review.sectionId,section_title:review.sectionTitle,expertise:review.expertise,issue:review.issue,proposal:review.proposal,reasoning:review.reasoning,expert_name:review.expertName||null,organization:review.organization||null,status:'new',client_payload:review})});if(!r.ok)throw new Error(`Central gemning fejlede (${r.status})`);return (await r.json())[0];}
export async function submitHandbookReview(input){const review={...input,id:id(),createdAt:new Date().toISOString(),schemaVersion:1};try{const row=await remoteInsert(review);return{central:true,row};}catch(error){const rows=drafts();rows.unshift({...review,localOnly:true,lastError:error.message});save(rows);return{central:false,error:error.message,review};}}
export function exportLocalHandbookDrafts(){const data={schemaVersion:1,exportedAt:new Date().toISOString(),reviews:drafts()};const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`ravradar-ekspertrettelser-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),0);}
export function localHandbookDraftCount(){return drafts().length;}
export async function listHandbookReviews(){const session=currentSession();if(!enabled||!session?.access_token)return[];const r=await fetch(`${PUBLIC_CONFIG.supabaseUrl}/rest/v1/handbook_reviews?select=*&order=created_at.desc`,{headers:{apikey:PUBLIC_CONFIG.supabasePublishableKey,Authorization:`Bearer ${session.access_token}`}});if(!r.ok)throw new Error(`Kunne ikke hente ekspertrettelser (${r.status})`);return r.json();}
export async function updateHandbookReview(id,patch){const session=currentSession();if(!enabled||!session?.access_token)throw new Error('Login kræves');const r=await fetch(`${PUBLIC_CONFIG.supabaseUrl}/rest/v1/handbook_reviews?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',headers:{apikey:PUBLIC_CONFIG.supabasePublishableKey,Authorization:`Bearer ${session.access_token}`,'Content-Type':'application/json',Prefer:'return=representation'},body:JSON.stringify(patch)});if(!r.ok)throw new Error(`Kunne ikke opdatere ekspertrettelse (${r.status})`);return (await r.json())[0];}
