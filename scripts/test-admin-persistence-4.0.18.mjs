import assert from 'node:assert/strict';import fs from 'node:fs';
const admin=fs.readFileSync(new URL('../js/ui/admin-dashboard.js',import.meta.url),'utf8');const store=fs.readFileSync(new URL('../js/services/admin-document-store.js',import.meta.url),'utf8');const update=fs.readFileSync(new URL('./update-weather.mjs',import.meta.url),'utf8');const hydrate=fs.readFileSync(new URL('./hydrate-deployed-weather.py',import.meta.url),'utf8');
assert.match(admin,/queueAdminDocumentSave\('water-level-station-routing'/);assert.match(admin,/queueAdminDocumentSave\('direction-reviews'/);assert.match(admin,/queueAdminDocumentSave\('rules'/);assert.match(store,/admin_documents/);assert.match(store,/localStorage/);assert.match(update,/stationRegistry = await dmiWaterStations/);assert.match(hydrate,/dmi-water-stations\.json/);assert.match(update,/const horizons=\[6,24,48,ACCEPTED_FORECAST_HOURS\]/);console.log('OK: central adminlagring, stationsregister-cache og udvidet diagnostik er koblet sammen.');
